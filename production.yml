version: "3.3"

services:
  nginx:
    image: registry.gitlab.com/nukapi/website:latest
    ports:
      - 80:80
    configs:
      - source: nginx
        target: /etc/nginx/nginx.conf
    deploy:
      replicas: 2

  form_api:
    image: registry.gitlab.com/nukapi/form_api:latest
    environment:
      RELAYHOST: 'smtp-relay.gmail.com'
      HOSTNAME: 'nukapi.com'
    deploy:
      replicas: 2

configs:
  nginx:
    file: ./conf/nginx/nginx.conf
